{
  "name": "my-excalidraw-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "MCP server for reading and writing workspaces in a self-hosted Excalidraw deployment.",
  "type": "module",
  "bin": {
    "my-excalidraw-mcp": "./build/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test test/**/*.test.mjs",
    "start": "node build/index.js",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "firebase-admin": "^13.0.0",
    "pako": "^2.1.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/pako": "^2.0.3",
    "typescript": "^5.6.0"
  }
}
